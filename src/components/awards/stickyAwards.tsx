import type { ReactNode } from 'react';
import type { AwardSeason } from '.';

/* Rendered for desktops to better view all awards */
const StickyAwards = ({ awards }: { awards: AwardSeason[] }): ReactNode => {
    return <div></div>
};

export default StickyAwards;
