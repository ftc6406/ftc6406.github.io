import type { ReactNode } from 'react';
import type { AwardSeason } from '.';

/* Simplified awards section that displays only one award for each year */
const MobileAwards = ({ awards }: { awards: AwardSeason[] }): ReactNode => {
  return <div></div>;
};

export default MobileAwards;
